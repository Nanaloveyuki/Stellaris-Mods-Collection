civspace_civilian_authority = {
	# Keep the civilian owner visible only as a minimalist contact profile.
	show_in_contacts_list = yes
	minimalist_contacts_list_profile = yes

	ai = {
		enabled = no
	}

	faction = {
		# Match vanilla selectable friendly special fleets (for example,
		# clutch_of_whelps). A globally neutral faction combined with a
		# per-player Friendly relationship creates a non-selectable hybrid.
		neutral = no
		hostile = no
		needs_border_access = no
		generate_borders = no
		needs_colony = no
		hostile_when_attacked = no
	}

	custom_diplomacy = yes

	modules = {
		basic_technology_module = {}
		standard_event_module = {}
		exclusive_diplomacy_module = {}
	}
}
